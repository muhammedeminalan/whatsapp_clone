import 'package:flutter/material.dart';

class AppColors {
  // Common Colors
  static const primary = Color(0xFF075E54);
  static const secondary = Color(0xFF25D366);
  static const error = Colors.red;

  // Light Theme Colors
  static const lightBackground = Color(0xFFECE5DD);
  static const lightSurface = Colors.white;
  static const lightTextPrimary = Color(0xFF111B21);
  static const lightTextSecondary = Color(0xFF3B4A54);
  // Dark Theme Colors
  static const darkBackground = Color(0xFF121B22);
  static const darkSurface = Color(0xFF1E2C33);
  static const darkTextPrimary = Colors.white;
  static const darkTextSecondary = Color(0xFFB0BEC5);
}
